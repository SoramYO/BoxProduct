header.header
    .container-fluid
        .row.align-items-center
            .col-3
                .inner-logo
                    a(href=`/${ADMIN_PATH}/dashboard`) ADMIN

            .col-9
                div(class="text-right")
                    a(
                        href=`/${ADMIN_PATH}/my-account`
                        class="btn btn-primary btn-sm mr-2"
                    ) AdminFullName
                    a(
                        href=`/${ADMIN_PATH}/auth/logout`
                        class="btn btn-danger btn-sm"
                    ) Đăng xuất